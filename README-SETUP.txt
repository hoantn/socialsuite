✅ SocialSuite Full Source Code (Minimal Starter)
-----------------------------------------------

📦 Cấu trúc đầy đủ Laravel controller + Vue Inertia Pages + routes + .env

📌 Cách dùng:
1. Tạo Laravel mới:
   => laravel new socialsuite && cd socialsuite
2. Giải nén ZIP này vào thư mục dự án (ghi đè)
3. Tạo file SQLite:
   => database/database.sqlite
4. Cài:
   => composer install
   => npm install
5. Migrate:
   => php artisan migrate --seed
6. Chạy:
   => php artisan serve --host=mmo.homes --port=8000