# --- SocialSuite dev hints (merge into your .env) ---
DB_CONNECTION=sqlite
DB_DATABASE=database/database.sqlite

# vite HMR over HTTP
VITE_HMR_HOST=localhost
VITE_HMR_CLIENT_PORT=5173
VITE_HMR_PORT=5173

# Optional quick test user token to call Graph API (paste a short-lived token)
# FB_USER_TOKEN=EAABwz... 
